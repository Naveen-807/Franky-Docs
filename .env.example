# ═══════════════════════════════════════════════════════════════
# FrankyDocs (DocWallet) — Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values.
#   cp .env.example .env
#
# Only GOOGLE_SERVICE_ACCOUNT_JSON and DOCWALLET_MASTER_KEY are
# required. All integrations are opt-in via their *_ENABLED flags.
# ═══════════════════════════════════════════════════════════════

# ── Required ──────────────────────────────────────────────────

# Google service account credentials (JSON string or file path).
# The account needs Docs + Drive API access.
GOOGLE_SERVICE_ACCOUNT_JSON=

# AES-256 master key for encrypting wallet private keys.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DOCWALLET_MASTER_KEY=

# ── Server ────────────────────────────────────────────────────

# HTTP_PORT=8787
# PUBLIC_BASE_URL=http://localhost:8787

# ── Document Discovery ────────────────────────────────────────

# Pin to a single doc (skip Drive discovery):
# DOCWALLET_DOC_ID=

# Discover all shared docs, not just those with the prefix:
# DOCWALLET_DISCOVER_ALL=0

# Name prefix filter for auto-discovered docs:
# DOCWALLET_NAME_PREFIX=[DocWallet]

# ── Polling Intervals (ms) ────────────────────────────────────

# POLL_INTERVAL_MS=15000
# DISCOVERY_INTERVAL_MS=60000
# BALANCE_POLL_INTERVAL_MS=60000
# SCHEDULER_INTERVAL_MS=30000

# ── Sui / DeepBook V3 ────────────────────────────────────────

# DEEPBOOK_ENABLED=0
# SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# ── Arc L1 (Chain 5042002) ────────────────────────────────────

# ARC_ENABLED=1
# ARC_RPC_URL=https://rpc.testnet.arc.network
# ARC_USDC_ADDRESS=0x3600000000000000000000000000000000000000

# ── Circle Developer-Controlled Wallets ───────────────────────

# CIRCLE_ENABLED=0
# CIRCLE_API_KEY=
# CIRCLE_ENTITY_SECRET=
# CIRCLE_WALLET_SET_ID=
# CIRCLE_BLOCKCHAIN=ARC-TESTNET
# CIRCLE_ACCOUNT_TYPE=SCA

# ── Yellow Network (NitroRPC/0.4) ─────────────────────────────

# YELLOW_ENABLED=0
# YELLOW_RPC_URL=
# YELLOW_WS_URL=
# YELLOW_APP_NAME=
# YELLOW_ASSET=ytest.usd

# ── WalletConnect v2 ──────────────────────────────────────────

# WALLETCONNECT_ENABLED=0
# WALLETCONNECT_PROJECT_ID=
# WALLETCONNECT_RELAY_URL=
